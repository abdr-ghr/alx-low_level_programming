#include "main.h"
/**
 * _abs - returns the absolute value of i
 * @i: the integer we get the abs value for
 * Return: The absolute value of i
 */
int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (i * -1);
}
